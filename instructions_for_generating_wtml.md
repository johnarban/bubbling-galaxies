1. Setup your parameters in `generate_wtml.py`
2. Run `python generate_wtml.py` to generate a new WTML file

If you need to trim or convert pixels to transparent. use `image_manip.py` to 
run the `imageMagick` commands to convert the images.